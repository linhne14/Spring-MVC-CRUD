apiVersion: v1
kind: Service
metadata:
  name: sso-app-service
  labels:
    app: sso-app
spec:
  selector:
    app: sso-app
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
  type: ClusterIP